<Page
    x:Class="Timesheet.App.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Timesheet.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator}, Path=LoginModel}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock Text="Timesheet" Margin="6" Style="{ThemeResource TitleTextBlockStyle}" />
        <Button Content="Log in" 
                Command="{Binding LoginCommand}"
                Width="300" Margin="6"
                HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>
